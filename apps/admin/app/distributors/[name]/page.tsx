"use client";

import Link from "next/link";
import { useParams } from "next/navigation";
import { useEffect, useMemo, useState } from "react";
import { useAdminSession } from "../../admin-session";
import { apiFetch, ApiRequestError } from "../../lib/api-client";

type Film = { movieId: string; title: string; posterUrl: string | null; active: boolean; dealStatus: "CURRENT" | "UPCOMING" | "PAST" | "UNSCHEDULED"; terms: Array<{ startWeek?: number; endWeek?: number | null; distributorShareBasisPoints?: number }>; showtimes: number; upcomingShowtimes: number; pastShowtimes: number; ticketsSold: number; ticketRevenueCents: number; fnbRevenueCents: number; distributorRevenueCents: number; cinemaRevenueCents: number; unallocatedRevenueCents: number; firstShowtime: string | null; lastShowtime: string | null };
type Distributor = { name: string; films: Film[]; showtimes: number; ticketsSold: number; ticketRevenueCents: number; fnbRevenueCents: number; distributorRevenueCents: number; cinemaRevenueCents: number; unallocatedRevenueCents: number };
type Report = { location: { name: string; timezone: string; currency: string }; distributor: Distributor };
type Period = "all" | "30" | "90" | "365";
const money = (cents: number, currency: string) => new Intl.NumberFormat("en-US", { style: "currency", currency }).format(cents / 100);

export default function DistributorPage() {
  const { name } = useParams<{ name: string }>();
  const { accessToken, employee } = useAdminSession();
  const [period, setPeriod] = useState<Period>("all"); const [report, setReport] = useState<Report | null>(null); const [error, setError] = useState<string | null>(null);
  const path = useMemo(() => { const base = `/reports/distributors/${encodeURIComponent(name)}`; if (period === "all") return base; const to = new Date(); const from = new Date(to.getTime() - Number(period) * 86_400_000); return `${base}?from=${encodeURIComponent(from.toISOString())}&to=${encodeURIComponent(to.toISOString())}`; }, [name, period]);
  useEffect(() => { let cancelled = false; setError(null); apiFetch<Report>(path, { accessToken }).then((result) => { if (!cancelled) setReport(result); }).catch((reason) => { if (!cancelled) setError(reason instanceof ApiRequestError ? reason.body.message : "Distributor performance could not be loaded."); }); return () => { cancelled = true; }; }, [accessToken, path]);
  if (!employee.permissions.includes("reports.view.financial")) return <main className="admin-route-page"><div className="error-banner">You do not have permission to view distributor performance.</div></main>;
  const distributor = report?.distributor; const currency = report?.location.currency ?? "USD";
  const dealCounts = distributor?.films.reduce((counts, film) => ({ ...counts, [film.dealStatus]: counts[film.dealStatus] + 1 }), { CURRENT: 0, UPCOMING: 0, PAST: 0, UNSCHEDULED: 0 }) ?? { CURRENT: 0, UPCOMING: 0, PAST: 0, UNSCHEDULED: 0 };
  const date = (value: string | null) => value ? new Date(value).toLocaleDateString([], { timeZone: report?.location.timezone, month: "short", day: "numeric", year: "numeric" }) : "—";
  return <main className="admin-route-page series-performance-page distributor-detail-page"><Link href="/distributors" className="back-link">← All distributors</Link><section className="series-performance-hero"><div><p className="kicker">DISTRIBUTOR PERFORMANCE</p><h1>{distributor?.name ?? name}</h1><p>Film performance, box-office allocation, cinema share, and every current, upcoming, past, or unscheduled deal.</p></div></section><div className="series-period-switch">{(["all", "30", "90", "365"] as Period[]).map((value) => <button type="button" className={period === value ? "active" : ""} onClick={() => setPeriod(value)} key={value}>{value === "all" ? "All time" : `${value} days`}</button>)}</div>{error && <div className="error-banner">{error}</div>}{!distributor && !error && <p className="dashboard-empty">Loading distributor performance…</p>}{distributor && <><section className="series-performance-metrics"><div><span>Films</span><strong>{distributor.films.length}</strong><small>{dealCounts.CURRENT} current · {dealCounts.UPCOMING} upcoming</small></div><div><span>Performances</span><strong>{distributor.showtimes}</strong><small>{distributor.ticketsSold} tickets</small></div><div><span>Ticket face value</span><strong>{money(distributor.ticketRevenueCents, currency)}</strong><small>{money(distributor.fnbRevenueCents, currency)} F&amp;B</small></div><div><span>Distributor share</span><strong>{money(distributor.distributorRevenueCents, currency)}</strong><small>Calculated from deal terms</small></div><div><span>Cinema share</span><strong>{money(distributor.cinemaRevenueCents, currency)}</strong><small>Film rental net</small></div><div><span>Needs terms</span><strong>{money(distributor.unallocatedRevenueCents, currency)}</strong><small>{dealCounts.UNSCHEDULED} unscheduled films</small></div></section><section className="panel distributor-deals"><div className="dashboard-section-heading"><div><p className="kicker">DEALS &amp; FILMS</p><h2>Complete film history</h2></div><span>{dealCounts.CURRENT} current · {dealCounts.UPCOMING} upcoming · {dealCounts.PAST} past</span></div><div className="distributor-film-table"><header><span>Film / deal</span><span>Status</span><span>Dates</span><span>Shows / tickets</span><span>Ticket / F&amp;B</span><span>Distributor / cinema</span></header>{distributor.films.map((film) => <article key={film.movieId}><span><Link href={`/films/${encodeURIComponent(film.movieId)}`}>{film.title}</Link><small>{film.terms.length ? film.terms.map((term) => `W${term.startWeek ?? "?"}${term.endWeek ? `–${term.endWeek}` : "+"}: ${((term.distributorShareBasisPoints ?? 0) / 100).toFixed(0)}%`).join(" · ") : "Deal terms not set"}</small></span><b className={`deal-status ${film.dealStatus.toLowerCase()}`}>{film.dealStatus.toLowerCase()}</b><span>{date(film.firstShowtime)}<small>to {date(film.lastShowtime)}</small></span><span>{film.showtimes} shows<small>{film.ticketsSold} tickets</small></span><span>{money(film.ticketRevenueCents, currency)}<small>{money(film.fnbRevenueCents, currency)} F&amp;B</small></span><span>{money(film.distributorRevenueCents, currency)}<small>{money(film.cinemaRevenueCents, currency)} cinema</small></span></article>)}</div></section></>}</main>;
}
